{
  "name": "protein-panda",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run start:dev --prefix backend\" \"npm run dev --prefix frontend -- -H 0.0.0.0\"",
    "migrate": "npm run --prefix backend prisma -- migrate deploy",
    "seed": "npm run --prefix backend prisma -- db seed"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
